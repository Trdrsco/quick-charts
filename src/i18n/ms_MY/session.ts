import type { Translation } from '../runtime'
import type { session as source } from '../en/session'

export const session: Translation<typeof source> = {
  'session.pre': 'Pra-pasaran',
  'session.open': 'Pasaran dibuka',
  'session.eth': 'Waktu elektronik',
  'session.after': 'Selepas waktu',
  'session.closed': 'Pasaran ditutup',
}
