import type { Translation } from '@trdrs/i18n'
import type { host as source } from '../en/host'

export const host: Translation<typeof source> = {
  'host.noVolume': 'ฟีดนี้ไม่มีข้อมูลปริมาณ',
  'host.replayHeader': '{tf} · การเล่นซ้ำ',
}
