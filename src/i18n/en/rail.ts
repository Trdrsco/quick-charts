// The built-in drawing rail: its buttons and their accessible names. Its three placement buttons are
// named from the shared tool vocabulary in `tools` — a tool is named once and translated once — so
// only the rail's OWN controls live here.
export const rail = {
  'rail.cursor': 'Cursor',
  'rail.deleteSelected': 'Delete selected drawing',
  'rail.clearAll': 'Clear all drawings',
} as const
