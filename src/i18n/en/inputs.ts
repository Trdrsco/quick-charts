// The indicator inputs editor. A field's name is derived from the manifest's own input key and its
// enum options come from the manifest itself — a definition's vocabulary, not the widget's.
export const inputs = {
  'inputs.cancel': 'Cancel',
  'inputs.apply': 'Apply',
} as const
