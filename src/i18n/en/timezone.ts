// The timezone picker. Zone ids, city names and offsets ("UTC-7") are the zone table's own data
// and carry no key; the picker's name and its exchange row are the chart's words.
export const timezone = {
  'timezone.title': 'Chart timezone',
  /** The row that follows the charted symbol's exchange clock. */
  'timezone.exchange': 'Exchange',
} as const
