import type { Translation } from '../runtime'
import type { rail as source } from '../en/rail'

export const rail: Translation<typeof source> = {
  'rail.cursor': 'Markör',
  'rail.deleteSelected': 'Ta bort markerad ritning',
  'rail.clearAll': 'Rensa alla ritningar',
}
