import type { Translation } from '../runtime'
import type { timezone as source } from '../en/timezone'

export const timezone: Translation<typeof source> = {
  'timezone.title': 'Grafik saat dilimi',
  'timezone.exchange': 'Borsa',
}
